export type HideOrderByShopTp = {
  is_show: boolean;
  code_shop: string;
  code_order: string;
};

export type RemoveOrderByShopTp = {
  code_shop: string;
  code_order: string;
  code_order_arr: string;
};
